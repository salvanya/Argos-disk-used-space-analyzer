# Current State — 2026-04-21

## In Progress
Brand logo integration (uncommitted).

## Last completed
- Commit 26d3100: revert lazy scanning (M14), restored recursive scan + WS progress.
- Session work (not yet committed):
  - Added `frontend/public/{logo.svg,isotipo.svg}` (copied from `logos/ISOTIPO.svg` + `LOGO.svg`).
  - `frontend/index.html`: favicon → `/isotipo.svg`.
  - `Header.tsx`: replaced `{t("app.name")}` text with `<img src="/logo.svg" h-12 w-auto />` (tagline kept). Bumped from h-7 after user said "too small".
  - `TopMenuBar.tsx`: added `<img src="/isotipo.svg" h-5 w-5 />` between back button and rescan, flanked by dividers.
  - Vite build succeeds; SVGs land in `backend/static/` root.

## Next step
Commit the logo integration as a single `feat:` commit — e.g. `feat(branding): integrate ISOTIPO + LOGO SVG assets`. Include `frontend/public/*.svg`, `index.html`, `Header.tsx`, `TopMenuBar.tsx`. Decide whether the top-level `logos/` directory (ISOTIPO.svg + LOGO.svg) should be committed as the canonical source or gitignored.

## Open questions
- Commit `logos/` as canonical source or keep out of repo?
- Any other surfaces that should show the logo (splash/loading, 3D graph header, empty states)?

## Files worth reloading next session
- `frontend/public/` (new — isotipo.svg, logo.svg)
- `frontend/src/components/layout/Header.tsx`
- `frontend/src/components/explorer/TopMenuBar.tsx`
- `frontend/index.html`
- `logos/{ISOTIPO,LOGO}.svg` (source files, untracked)
