# Current State — 2026-04-16

## In Progress
Nothing — M1 just completed and committed.

## Last Completed
- Commit 3cb33f7: M1 scanner core (recursive scanner, SQLite cache, Windows utils)
  - `backend/core/scanner.py` — DiskScanner, synchronous walk, symlink exclusion, PermissionError handling
  - `backend/core/cache.py` — ScanCache, SQLite JSON blob, UPSERT, CacheError propagation
  - `backend/core/windows_utils.py` — is_link(), is_admin(), is_hidden(), is_system_file()
  - `backend/core/models.py` — NodeType (StrEnum), ScanNode (recursive Pydantic v2), ScanResult, ScanOptions
  - 50 tests, 4 skipped (symlinks need Developer Mode on this machine), 94.3% coverage

## Next Step
Start M2 — API layer. Run `/plan M2 API layer` first.
Key constraints:
- FastAPI endpoints for scan trigger + result retrieval
- WebSocket for progress streaming (wrap DiskScanner.scan in run_in_executor)
- localhost 127.0.0.1 binding only, token auth required on all scan/filesystem routes
- ScanCache used for persisting and retrieving results
- Use ASGITransport in integration tests (see lessons/httpx-asgi-transport.md)

## Open Questions
- None blocking M2.

## Files Worth Reloading Next Session
- backend/core/scanner.py — public API: DiskScanner.scan(root, options, progress_callback)
- backend/core/cache.py — public API: ScanCache(db_path).get/put/delete/list_roots
- backend/core/models.py — ScanNode, ScanResult, ScanOptions
- specs/m1-scanner-core/plan.md — for reference on design decisions made
