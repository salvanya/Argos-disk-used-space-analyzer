# Current State — 2026-04-16

## In Progress
Nothing — M2 just completed and committed.

## Last Completed
- Commit e52bf59: M2 API layer (HTTP + WebSocket scan API, token auth, 18 tests, 94.1% cov)

## Next Step
Start M3 — Home screen (React frontend).
Run `/plan M3 home screen` first.
Key constraints:
- Folder picker (native dialog via File System Access API or input[type=file])
- Recent folders list (GET /api/scans → list of ScanSummary)
- Admin detection indicator (pywin32 IsUserAnAdmin — expose via a new GET /api/system endpoint)
- "Scan as administrator" button (ShellExecuteW runas — M12 stretch, but UI placeholder needed)
- Glassmorphism + aurora gradient aesthetic (see CLAUDE.md §5)

## Open Questions
- Admin detection endpoint: implement minimal GET /api/system/info in M3 or defer to M12?
  (Recommend: add minimal endpoint in M3 so the UI can show the shield icon.)

## Files Worth Reloading Next Session
- backend/api/scan.py — WS /ws/scan, GET /api/scans, GET/DELETE /api/scan/{root_b64}
- backend/api/dependencies.py — verify_token, get_cache
- backend/core/models.py — all models incl. ScanStartRequest, ScanSummary, WsXxxMessage
- frontend/src/ — not yet created (M3 starts it)
- specs/m2-api-layer/plan.md — API surface decisions
