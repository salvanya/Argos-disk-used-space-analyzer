# Current State — 2026-04-19

## In Progress
None. Planning-only session. Two backlog milestones specified but deferred by user: "we will not work on these fixes in this session."

## Last Completed
- Commit b809a05: `docs(memory): lesson — fnmatch treats ** like *, not gitignore-style` (pre-session; no new commits this session).
- This session added: `specs/m13-ux-refinements/spec.md`, `specs/m14-lazy-scanning/spec.md`, extended `CLAUDE.md §10` with M13 + M14 entries. **Uncommitted.**

## Next Step
Next session: pick M13 or M14 (user-driven priority; no ordering constraint). Before any code:
1. Ask user to resolve **M14 Open Question #1** — pure lazy vs lazy-first-+-background-refine. This is the only hard blocker.
2. Ask user to confirm/override M13's five defaults (sort-reset boundary, handle visual, footer scope, dropdown-fix scope, sphere-formula constants).
3. Run `/plan` per chosen milestone, then `/tdd`.

Also: commit the three unstaged doc changes (two specs + CLAUDE.md edit) if user approves — currently uncommitted on `main`.

## Open Questions
- M14 flavor (pure lazy vs hybrid refine) — user punted.
- M14 fate of `POST /api/scan` — default proposal: route to `scan_level(root)` for back-compat.
- Full list lives in each spec's "Open Questions" section.

## Files Worth Reloading Next Session
- `specs/m13-ux-refinements/spec.md` — 6 UX items, 5 open questions with default proposals.
- `specs/m14-lazy-scanning/spec.md` — lazy-scan architectural reversal, 8 open questions.
- `CLAUDE.md §2.4` — the "complete scan (not lazy/progressive)" line M14 must rewrite.
- `CLAUDE.md §10` — milestones list now includes M13 + M14.
- `backend/core/scanner.py` — `DiskScanner.scan()` will become `scan_level()` in M14.
- `backend/core/cache.py` — `ScanCache` schema changes for per-folder partial results in M14.
