---
description: Write a formal feature spec following the Spec-Driven Development methodology
argument-hint: <feature name and short description>
---

Create a formal spec for:

**$ARGUMENTS**

Steps:

1. **Trigger the `spec-driven-development` skill.**
2. Write `specs/<slug>/spec.md` using the SDD spec template:
   - Problem
   - Goals
   - Non-Goals
   - User Stories
   - Acceptance Criteria (Given/When/Then format — MUST be testable)
   - Open Questions
3. Show me the spec in chat.
4. If there are Open Questions, ask me to resolve them before proceeding.

**Do NOT write a plan or code yet.** The spec comes first.
